# Stocks Website

A Flask website displaying live stock prices for SPX, NVDA, MSFT, AAPL, TSLA, and IBIT.

## Setup

```bash
cd projects/stocks-website
pip install -r requirements.txt
python app.py
```

Then open `http://localhost:5000` in your browser.
